# P6Spy configuration
# Route all logging through our custom listener — disable all built-in output
appender=com.benchmark.P6SpyQueryListener

# Disable log file output
logfile=

# Do not log slow queries
outagedetection=false

# Use the real PostgreSQL driver underneath
driverlist=org.postgresql.Driver
